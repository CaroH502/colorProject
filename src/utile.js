import * as convert from "color-convert"


export const hextoHSL = ( input ) => {
    constInputString = input.toString();
    console.log(convert.hex.hsl(input));
}

//faire la fonction qui fait la palette de couleurs
